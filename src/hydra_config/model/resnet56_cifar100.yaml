defaults:
  - auxiliary: resnet56_cifar100

name: resnet56_cifar100

client:
  epoch      : 20
  batch_size : 256
  options:
  # optimizer:
  #   name: adam
  #   options:
  #     lr          : 0.003
  #     weight_decay: 0.0001
  #     betas       : [0.9, 0.999]
  lr_scheduler:
    milestones : [100, 150]

server:
  options:
    num_classes : 100

  # optimizer:
  #   name: adam
  #   options:
  #     lr          : 0.003
  #     weight_decay: 0.0001
  #     betas       : [0.9, 0.999]
  lr_scheduler:
    milestones : [100, 150]
